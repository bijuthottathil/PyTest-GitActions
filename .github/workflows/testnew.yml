name : mydeployment
on: 
  workflow-dispatch:

jobs:
  build:
    runs on : ubuntu:latest

    steps: 
      name : Checkout code
      uses : action/Checkout@v4   

    name : setup Python
    uses : actions/setup-python@v4 
